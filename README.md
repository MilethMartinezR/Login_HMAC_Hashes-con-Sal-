# Login_HMAC_Hashes-con-Sal-
Sistema de autenticación web con Flask que implementa HMAC-SHA256 para verificar la integridad de mensajes. Incluye gestión de usuarios con hash SHA-256 + salt y timestamps.
